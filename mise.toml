[tools]
node = "22"
python = "3.12"
uv = "latest"

[tasks.bootstrap]
description = "Install project dependencies"
run = "uv sync --directory packages/wake-word"

[tasks.wakeword]
description = "Run live wake word detection test"
run = "cd packages/wake-word && uv run python src/run.py"